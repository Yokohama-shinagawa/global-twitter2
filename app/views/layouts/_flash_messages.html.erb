<% flash.each do |message_type, message| %>
	<% perm = ['success', 'warning', 'danger'] %>
	<% type = perm.include?(message_type) ? message_type : 'warning' %>
	<div class ="alert alert-<%= type %>" style="margin: 0px 0px 20px;"><%= message %></div>
<% end %>


